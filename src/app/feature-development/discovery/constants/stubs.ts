/**
 * @fileoverview Re-export shim — preserves the existing import path.
 *
 * All stub logic has been moved into the `./stubs/` domain folder.
 * This file exists solely for backward compatibility so that existing imports
 * of `'../constants/stubs'` continue to resolve without changes.
 *
 * @see `./stubs/index.ts` for the full module map and documentation.
 */
export * from './stubs/index';
