{
  "name": "voice-relay",
  "version": "0.1.0",
  "description": "Mac-side relay: voice transcript -> resolve tech-lead-stack skill/workflow -> drive an agent CLI (agnostic) -> gated propose/approve/apply. Keyless; rides your subscription logins.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "NODE_ENV=test tsx --test src/**/*.test.ts",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "eval": "tsx scripts/eval-local-model.ts",
    "setup:dashboard": "tsx scripts/setup-dashboard.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/supertest": "^7.2.1",
    "supertest": "^7.2.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
