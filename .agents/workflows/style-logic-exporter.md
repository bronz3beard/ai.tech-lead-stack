---
description: Extract style logic and tokens for Figma
---

// turbo-all

1. Call the tech-lead-stack.get_skills tool:
   - skillName: "style-logic-exporter"
   - projectName: "<YOUR_CURRENT_PROJECT_NAME>" (e.g., "gilly")
   - model: "<YOUR_MODEL_NAME>" (e.g., "gemini-1.5-pro")
   - agent: "<YOUR_AGENT_NAME>" (e.g., "Antigravity")

2. Follow the workflow to execute style discovery, extract the design system context, and format it for Figma Make prompts.
